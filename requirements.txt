# Core workflow uses Python >=3.9 standard library.
# Optional: install pdfplumber for exact-quotation highlights in --render-pages packages.
# System dependency for PDF processing: Poppler (pdfinfo, pdftotext, pdftoppm).
